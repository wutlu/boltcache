package appinfo

import (
	"time"
)

var (
	Name = "boltcache"
	Title = "BoltCache"
	Description =  "A lightweight, high-performance in-memory caching server written in Go."

	// Application version
	Version = "1.0.0"

	StartTime = time.Now()
)
